# Basic component tab

- [Resistor palette button](resistor.md)

This first case does not cover the other palette buttons. Each registry entry
needs an explicit scenario instance with its expected symbol, pins and value.
See the [scenario standard](../../README.md).
